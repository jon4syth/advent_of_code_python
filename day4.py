# input = open("input", "r")

class Day4():
    def read_file(file_name):
        f = open(file_name, "r")
        lines = f.readlines()
        return lines

    def count_valid_passports(lines):
        """
        byr (Birth Year)
    iyr (Issue Year)
    eyr (Expiration Year)
    hgt (Height)
    hcl (Hair Color)
    ecl (Eye Color)
    pid (Passport ID)
    cid (Country ID)
        :return:
        """
        return 2
